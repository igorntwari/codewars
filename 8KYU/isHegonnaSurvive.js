function hero(bullets, dragons){
    //Get Coding!
      // calculate the total bullets needed to defeat the dragons 
      // return the bullets which are higher or equal to the calculated bullets 
      
      const bulletsToDefeat = dragons * 2;
      return bullets >= bulletsToDefeat;
    }